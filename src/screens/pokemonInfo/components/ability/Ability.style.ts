import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    textTransform: "capitalize",
    fontSize: 16,
    fontWeight: "600",
  },
  container: {
    marginBottom: 20,
  },
});
